(function () { const pTags = Array.from(Array.from(document.querySelectorAll("p")).filter((x) => { return x.innerText === "Tags" })).at(0); const parent = pTags.parentElement.nextElementSibling.firstElementChild; const children = Array.from(parent.children); children.sort((n1, n2) => { const tag_count = (n) => { return (function () { const x = Array.from(n.children).at(1); return parseInt(x.innerText.replaceAll(",", "")) })() };; return (tag_count(n1) - tag_count(n2)) }); while (parent.firstChild) {parent.removeChild(parent.firstChild)}; return children.forEach((child) => { return parent.appendChild(child) }) })()