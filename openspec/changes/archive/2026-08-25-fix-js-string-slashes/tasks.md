## 1. Генерация JavaScript

- [x] 1.1 Разделить генерацию строковых литералов и преобразование namespace-символов в `backend_compiler/js.ml`; проверить, что `(str "/")` генерирует `(str)("/")`, а `alias/member` остаётся `alias.member`.

## 2. Регрессионная проверка

- [x] 2.1 Добавить в `test/js_ns_test.ml` проверку строкового литерала со слешем и убедиться, что `dune exec ./test/js_ns_test.exe` проходит.

## 3. Полная проверка

- [x] 3.1 Выполнить `make test` и убедиться, что полный набор тестов проходит.
