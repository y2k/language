## 1. Общий lookup и get-in

- [x] 1.1 В `backend_eval/eval_stdlib.ml` добавить nil-случай в `get`, реализовать `get-in` свёрткой через него с проверкой типа пути и зарегистрировать binding; проверить evaluator-вызовы `(get nil :text)`, `(:text nil)`, смешанного и пустого пути через существующий runner.
- [x] 1.2 В `prelude/language_runtime.js` добавить nil-случай в `get`, экспортировать `get_in` с проверкой пути и свёрткой через `get`, добавить импорт в `backend_compiler/js.ml`; проверить, что compiled source со смешанным путём выполняется и возвращает `42`, а пустой путь сохраняет исходное значение.
- [x] 1.3 В `prelude/language_runtime.java` добавить nil-случай в `get` и `get_in` с проверкой пути и циклом через `get`; проверить компиляцию и выполнение generated Java для nil, смешанного и пустого пути через существующий static import.

## 2. Проверки контракта

- [x] 2.1 Добавить общий fixture `test/samples/get_in.clj` с ожидаемым выводом на первой строке: все примеры #17, путь из переменной/функции, индекс за границей, пустой путь с `false`, регрессии обычного map/list `get`; проверить одинаковый результат на eval, JS и Java через sample suite. Не закреплять list-пути, некорректные индексы или nil-destructuring.
- [x] 2.2 Добавить целевые проверки ошибок в существующем формате OCaml/Alcotest: промежуточные число/строка/boolean и неверный тип пути (`nil`, map, строка, число, boolean), включая исходный `nil`; проверить отказ на всех трёх backend, не требуя одинакового текста исключений и не принимая ошибку компиляции вместо runtime-проверки.
- [x] 2.3 Добавить JS runtime-проверку чтения обычного object с отсутствующим свойством и явным `undefined`, а также `false`, `0` и `""`; проверить соответственно `null` и сохранение существующих значений существующим способом запуска Node из тестов.

## 3. Интеграционная проверка

- [x] 3.1 Выполнить `ocamlformat -i` на изменённых `.ml`/`.mli`, затем `make test`; завершение подтверждается успешной сборкой и прохождением всех тестов, включая новые общие и целевые сценарии.
