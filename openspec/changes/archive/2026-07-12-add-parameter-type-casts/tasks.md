## 1. Дешугорирование параметров

- [x] 1.1 Расширить `fn_macro` в `frontend/builtin_macros.ml`: заменять каждый аннотированный символьный параметр на значение из `Gensym.gensym` и добавлять initial `let*` с `(cast TYPE FRESH)`.
- [x] 1.2 Добавить frontend-тесты для одного и нескольких аннотированных параметров, а также для `defn`.

## 2. Семантика cast по целям

- [x] 2.1 Обработать `(cast TYPE value)` в `backend_eval/eval.ml` как no-op core-форму и добавить eval-тест.
- [x] 2.2 Обработать `(cast TYPE value)` в `backend_compiler/js.ml` как прозрачное выражение и добавить JS-тест генерации.
- [x] 2.3 Добавить Java-тест, подтверждающий генерацию валидного локального параметра с Java cast.

## 3. Проверка

- [x] 3.1 Отформатировать изменённые `.ml` файлы через `ocamlformat -i`.
- [x] 3.2 Запустить `make test`.
