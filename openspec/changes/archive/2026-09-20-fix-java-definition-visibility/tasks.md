# Tasks

## 1. Генерация видимости

- [x] 1.2 Переиспользовать разрешение квалифицированных имён при чтении Java значений; проверить alias/member в возврате и атомарном RHS поля, а также сохранение строк с `/` и приоритета локальных bindings.

- [x] 1.1 В `backend_compiler/java.ml` использовать metadata core `def` для выбора `public static` или `private static` у методов и обеих ветвей генерации полей; обновить ожидаемые сигнатуры в `test/java_ns_test.ml` и добавить проверки public/private с `ns` и без него, включая атомарные и составные RHS. Проверка: `dune exec ./test/java_ns_test.exe` проходит и покрывает все три пути генерации.

## 2. Интеграционные проверки Java

- [x] 2.1 Добавить интеграционные тесты межпакетного вызова reproducer #23 и чтения публичных полей с простым и составным RHS, подключив тест к `test/dune` при необходимости. Проверка: generated Java совместно с `RUNTIME_JAVA` успешно компилируется через `javac`, исполнение возвращает ожидаемые значения и сохраняет порядок инициализации.
- [x] 2.2 Проверить вызов приватной функции и чтение приватных полей внутри своего namespace, а также делегирование метода `gen-class` в приватную реализацию. Проверка: интеграционные fixtures успешно компилируются и исполняются с ожидаемым результатом.
- [x] 2.3 Добавить отдельные отрицательные проверки внешнего обращения к приватному методу и полю из того же и другого Java package. Проверка: после успешной сборки валидного fixture каждый из четырёх случаев отклоняется `javac` из-за доступа к проверяемому члену, без сравнения полного текста диагностики.

## 3. Документация и общая проверка

- [x] 3.1 Обновить английский справочник `skills/y2k-language/SKILL.md`: отразить public/private Java members, межпакетный доступ и миграцию внешних обращений к `defn-`/`def-`. Проверка: прежние утверждения о package-private публичных членах и отсутствии Java private modifiers заменены контрактом из delta spec.
- [x] 3.2 Выполнить `ocamlformat -i` для изменённых `.ml`/`.mli` и полную проверку `make test`. Проверка: сборка и все тесты проходят, включая новые интеграционные сценарии и существующие Java snapshots.
