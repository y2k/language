## 1. Поддержка синтаксиса

- [x] 1.1 Изменить `frontend/macro_ns.ml`, чтобы `:import` принимал один или несколько bracket-векторов и создавал прежние import pairs в исходном порядке.
- [x] 1.2 Сохранить валидацию package и class symbols, а также поведение существующей одно-векторной формы.

## 2. Проверки

- [x] 2.1 Добавить в `test/frontend_desugar_test.ml` проверку дешугорирования двух import-векторов в одном `:import` clause.
- [x] 2.2 Добавить Java sample, который использует классы из двух импортированных пакетов и проходит существующий `javac`/`java` harness с детерминированным результатом.

## 3. Верификация

- [x] 3.1 Применить `ocamlformat -i` к изменённым OCaml-файлам.
- [x] 3.2 Выполнить `make test`.
