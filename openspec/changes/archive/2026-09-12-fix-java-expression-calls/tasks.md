## 1. Генерация вызова

- [x] 1.1 В `backend_compiler/java.ml` добавить безусловное приведение вызываемого выражения к `FnN` через существующий `fn_interface`, используя арность и metadata внешней формы, без исключений для inline-лямбд и `(cast TYPE ...)`; включать выражение в output один раз и пояснить сознательное повторное приведение комментарием `ponytail:`. Проверить через CLI, что примеры `direct` и `callback` из #19 получают cast перед внешним `.call(...)`.

## 2. Регрессионные проверки

- [x] 2.1 Добавить общий sample `test/samples/fn_expression_calls.clj` с фабриками арностей 0–4, callback-цепочкой, результатами `get`/`if`, atom-счётчиком однократного вычисления и контрольными локальным вызовом и inline-лямбдой. Аргументы не создают взаимодействующих побочных эффектов: счётчик проверяет число вызовов фабрики, а не порядок эффектов. Первая строка задаёт ожидаемый вывод; критерием служит одинаковый результат этих сценариев на eval, JavaScript и Java в итоговом `make test`.
- [x] 2.2 Добавить Java-целевой sample `test/samples/java/fn_expression_cast.clj` для вызова результата фабрики с явным `cast Fn1` и прямого `(.call (cast java.util.concurrent.Callable x))` на типизированной `Callable`-лямбде; критерием служат успешные `javac` и исполнение с ожидаемым результатом в итоговом `make test`.
- [x] 2.3 Добавить в `test/java_ns_test.ml` проверку вызова результата выражения с пятью аргументами: Java-генерация должна выдавать ошибку об арности `5` с координатами внешнего вызова. Проверить через `dune exec ./test/java_ns_test.exe`.
- [x] 2.4 В `test/java_ns_test.ml` проверить внешний cast к `Fn1` для явного `(cast Fn1 ...)`, внешний cast к `Fn0` для `((cast java.util.concurrent.Callable x))` и отсутствие такого cast у прямого `(.call (cast java.util.concurrent.Callable x))`; проверять существенные выражения без snapshot всего класса. Проверить через `dune exec ./test/java_ns_test.exe`.

## 3. Итоговая проверка

- [x] 3.1 Выполнить `ocamlformat -i backend_compiler/java.ml test/java_ns_test.ml`, затем `make test`; убедиться, что новые samples проходят на соответствующих targets, целевой тест диагностики проходит и существующие проверки Java interop и локальных функций остаются успешными.
