## 1. Eval runtime

- [x] 1.1 Добавить `Atom of value ref` в `backend_eval/eval_types.ml`, обработать новый вариант в диагностике и строковом представлении без рекурсивного обхода содержимого; проверить успешный `dune build` и безопасный вывод самоссылки.
- [x] 1.2 Реализовать и зарегистрировать `atom`, `deref`, `reset!`, `swap!` в `backend_eval/eval_stdlib.ml` с фиксированными арностями, проверками ссылки и вызовом callback через существующий `apply`; проверить через eval создание, чтение, возвращаемые и сохранённые значения, а также сохранность ячейки после ошибки callback.

## 2. Compiler runtimes

- [x] 2.1 Добавить представление Atom и четыре функции в `prelude/language_runtime.js`, проверки арности через `arguments.length`, типа ссылки и вызываемости callback; проверить через Node успешное обновление, отсутствие записи при исключении и отклонение лишних аргументов.
- [x] 2.2 Добавить `atom`, `deref`, `reset_BANG_`, `swap_BANG_` в существующий import `backend_compiler/js.ml`, обновить ожидаемые imports в `test/js_ns_test.ml`; проверить `dune exec ./test/js_ns_test.exe` и наличие этих имён в сгенерированном модуле.
- [x] 2.3 Добавить ячейку Atom и по одному методу `atom`, `deref`, `reset_BANG_`, `swap_BANG_` в `prelude/language_runtime.java`, используя `call_fn` для callback и запись только после успешного возврата; проверить через `javac`/`java` обычную языковую лямбду с захватом, обновление и сохранность значения после исключения.

## 3. Интеграционное покрытие

- [x] 3.1 Добавить общий `test/samples/atoms.clj` с ожидаемой первой строкой и top-level `test`; покрыть все четыре операции, их результаты и последующие чтения, alias и передачу через параметр, независимость ячеек, захват локального значения и однократность callback. Дополнить сценарий хранением `nil`, `false`, коллекций и функции, а также `reset!` на falsey-значения; проверить одинаковый строковый результат в sample-группах `eval`, `js`, `java`.
- [x] 3.2 Добавить компактные исполняемые негативные проверки из `specs/atoms/spec.md` на всех targets: недостаточные/лишние аргументы, неподходящая ссылка, невызываемый callback и исключение callback с последующим чтением прежнего значения. Использовать Alcotest, Node и `javac`/`java`; при добавлении `test/atoms_test.ml` зарегистрировать его в `test/dune`. Проверить, что каждый ошибочный вызов действительно отклоняется, а проверки состояния исполняются после перехвата ошибки на host-стороне.

## 4. Общая проверка

- [x] 4.1 Запустить `ocamlformat -i` для всех изменённых `.ml`/`.mli`, затем `make test`; проверить успешную сборку, прохождение общего Atom sample на трёх targets, негативных проверок и существующего набора тестов.
