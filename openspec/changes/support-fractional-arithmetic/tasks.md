# Tasks

Расположение runtime уточнено и подтверждено пользователем: задачи 2.3 и 2.4 меняют файлы `/Users/igor/Projects/packages/prelude/1.0.0/java/language_runtime.java` и `/Users/igor/Projects/packages/prelude/1.0.0/js/language_runtime.js` через симлинки `prelude/` репозитория language. Итоговый diff проверен в обоих репозиториях; `make test` прошёл с этими изменениями.

## 1. Интеграционный контракт

- [x] 1.1 Добавить `test/samples/fractional_arithmetic.clj` со сценариями дробных и смешанных `+`, `-`, `*`, отрицательных значений, нескольких аргументов, вложенных вычислений и `reduce` с явной лямбдой; проверить наличие ожидаемого вывода в первой строке и регистрацию sample на всех targets существующим harness.
- [x] 1.2 Включить в sample вывод `1.2 0.4` для веса `0.2`, нормализацию целых результатов всех трёх операций, отрицательного целого и нуля через `str`, `=` и `not=`; проверить соответствие каждой ожидаемой строки сценариям `fractional-arithmetic`.
- [x] 1.3 Дополнить целочисленные samples пустыми и одноаргументными формами и `(/ 7 2)`; добавить точечную проверку ошибки `(-)` на targets существующими средствами тестирования. Проверить ожидаемые `0`, `1`, неизменный аргумент унарного `-`, частное `3` и ошибку вместо значения для пустого `-`.

## 2. Арифметика и представление результатов

- [x] 2.1 В `backend_eval/eval_stdlib.ml` добавить дробный путь для `+`, `-`, `*`, сохранив целочисленный путь и не расширяя общий `to_int` для других операций; проверить новые eval-сценарии и сохранение целочисленных результатов.
- [x] 2.2 Нормализовать целые результаты eval и реализовать сохраняющее binary64-значение форматирование дробных результатов стандартными средствами; добавить точечную проверку round-trip для значения, требующего более 12 значащих цифр, и проверить `str`, `=` и вложенную арифметику из sample.
- [x] 2.3 В `prelude/language_runtime.java` поддержать дробный путь и возврат `Integer` для нормализованного целого либо `Double` для дробного результата; проверить компиляцию generated Java и новые сценарии через JVM, включая равенство результата целому литералу.
- [x] 2.4 В `prelude/language_runtime.js` нормализовать нулевые результаты `+`, `-`, `*` без изменения глобального равенства; проверить общий sample через Node, особенно `(* -0.5 0)`, его вывод и равенство `0`.

## 3. Документация и полная проверка

- [x] 3.1 Обновить English reference `skills/y2k-language/SKILL.md`: поддержку дробных `+`, `-`, `*`, смешанных аргументов, нормализацию целых результатов, binary64-точность, границы форматирования и сохраняющиеся ограничения `/` и сравнений; сверить примеры с принятыми specs и тестами, не объявляя issue #22 полностью решённым.
- [x] 3.2 Выполнить `ocamlformat -i` на изменённых `.ml`/`.mli`, затем `make test`; завершить задачу только после успешной сборки и всех тестов, включая новые samples на eval, JS и Java без пропусков или ожидаемых падений.
