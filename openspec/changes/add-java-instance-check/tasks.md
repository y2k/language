## 1. Java-форма и lowering

- [ ] 1.1 Добавить узкую обработку `instance?` в `backend_compiler/lowering_expression_to_statement.ml`: сохранять операнд типа, арность и исходные metadata, нормализовать значение, в discard-позиции использовать binding через `Gensym`. Проверить в `test/lowering_test.ml`, что TYPE-выражения не упрощаются и отброшенная проверка сохраняет вычисление значения.
- [ ] 1.2 Добавить генерацию `(((Object) VALUE) instanceof TYPE)` и диагностику арности, literal/несимвольных и primitive TYPE в `backend_compiler/java.ml`. Проверить в `test/java_ns_test.ml` наличие нативного `instanceof`, использование imports и полных имён, отсутствие reflection и ошибки с исходной позицией, включая `(do String)` в позиции TYPE.

## 2. Остальные targets и исполняемый контракт

- [ ] 2.1 Добавить явный отказ для `instance?` в `backend_compiler/js.ml` и `backend_eval/eval.ml`. Проверить диагностикой целевого теста, что сообщение называет форму и Java-only ограничение, а eval не выполняет эффект аргумента.
- [ ] 2.2 Добавить компактный `test/instance_check_test.ml` и зарегистрировать его в `test/dune`, используя существующий подход с `RUNTIME_JAVA`, `javac` и Java runner. Проверить импортированный и полный тип, подкласс, интерфейс, `nil`, несовместимые final-классы и boxing скаляров по delta-spec; неизвестный класс должен приводить к отказу настоящего `javac`.
- [ ] 2.3 В том же suite проверить однократность через atom-счётчик, значение с `if`/`let`/`do`, использование проверки в условии, return, аргументе и binding, discard-позицию и отсутствие эффектов в невыбранной ветви. Критерий: generated Java компилируется и runtime-проверки результатов и счётчиков проходят.

## 3. Справочник и общая проверка

- [ ] 3.1 Обновить английский `skills/y2k-language/SKILL.md`: синтаксис специальной формы, статический TYPE, Java-only ограничение, `nil`/boxing, отсутствие автоматического сужения типа и проверка неизвестных классов через `javac`. Сверить описание с delta-spec и примерами исполняемого suite.
- [ ] 3.2 Выполнить `ocamlformat -i` для изменённых `.ml`/`.mli`, затем `make test`; убедиться, что проходят новый suite и существующие проверки всех targets.
